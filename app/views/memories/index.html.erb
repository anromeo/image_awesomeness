Welcome <%= current_user.email %>
<%= link_to "Create a New Memory", new_memory_path %>
<% @memories.each do |mem| %>
  <%= link_to mem.title, mem %>
  <% mem.images.each do |pic| %>
    <%= image_tag pic.photo_url(:thumb).to_s %>
  <% end %>
<% end %>